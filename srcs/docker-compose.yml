version: "3.3"

networks:
    inception:

#volumes:

services:
# https://hub.docker.com/_/nginx
    nginx:
        container_name: nginx
        build: ./requirements/nginx
        image: nginx:nginx
#       depends_on:
#           - wordpress
        restart: always
        networks: inception
        ports:
            - "443:443"
#        volumes:
#           -

#	wordpress:

#	mariadb:
